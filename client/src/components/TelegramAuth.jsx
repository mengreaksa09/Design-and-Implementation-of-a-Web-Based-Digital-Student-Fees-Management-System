const TelegramAuth = ({ children }) => {
  // Simply return children without any Telegram logic for now
  // Telegram authentication will be handled separately
  return children;
};

export default TelegramAuth;
